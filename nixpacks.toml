# Nixpacks build configuration for Railway
[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libGL", "glib"]

[phases.install]
cmds = [
    "python -m pip install --upgrade pip",
    "pip install -r backend/requirements.txt"
]

[start]
cmd = "cd backend 2>/dev/null && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} || uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
